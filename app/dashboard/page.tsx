// import { getProtectedDataAction } from "../actions/protected.actions";

export default async function DashboardPage() {
  // const data = await getProtectedDataAction();

  return (
    <div>
      <h1>Dashboard</h1>
      {/* <p>{data.message}</p> */}
    </div>
  );
}
